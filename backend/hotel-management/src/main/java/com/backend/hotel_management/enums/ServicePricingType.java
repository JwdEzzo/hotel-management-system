package com.backend.hotel_management.enums;

public enum ServicePricingType {
   PER_ORDER, // Movie tickets, spa treatments, room service
   PER_HOUR, // Gym access, meeting room rental
   PER_NIGHT // NEW: Services charged per night of stay
}